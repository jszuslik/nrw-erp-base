import {Base} from '../base';

export class Lead extends Base {
    name: string;
    image: string;
    company: string;
    email: string;
    phone: string;
    source: string;
    owner: string;
}
